# Prometheus step executor

The Prometheus step executor supports the tracking of pipeline or step metrics with [Prometheus](https://prometheus.io/).

::: wurzel.step_executor.prometheus_executor
    handler: python
